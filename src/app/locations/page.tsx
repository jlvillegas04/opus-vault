import { LocationList } from "@/components/locations/location-list";

export const metadata = { title: "Locations | Opus Vault" };

export default function LocationsPage() {
  return <LocationList />;
}
