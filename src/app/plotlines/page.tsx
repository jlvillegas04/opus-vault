import { PlotlineList } from "@/components/plotlines/plotline-list";

export const metadata = { title: "Plotlines | Opus Vault" };

export default function PlotlinesPage() {
  return <PlotlineList />;
}
