import { CharacterList } from "@/components/characters/character-list";

export const metadata = { title: "Characters | Opus Vault" };

export default function CharactersPage() {
  return <CharacterList />;
}
